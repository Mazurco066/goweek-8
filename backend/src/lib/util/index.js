// Inner modules
const baseResponse = require('./baseResponse')
const generateField = require('./generateField')
const immutable = require('./immutable')
const proccessing = require('./proccessing')
const validation = require('./validation')

// Exports
module.exports = {
    baseResponse,
    generateField,
    immutable,
    proccessing,
    validation
}